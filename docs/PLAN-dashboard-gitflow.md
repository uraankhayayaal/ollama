TODO: У каждой задачи есть своя ветка, таке свои коммиты
    - Нужно проверять, что если ветка находится в другой, то учитывать.
    - Если ветка задачи залита в эпик, то задача готова.
    - Если эпик со всеми задачами залита в main, то эпик готов.
    - Если коммитов нет, то кнопку создания МР не нужно показывать и делать запрос на сервер (нужно показать что еще нет коммитов).
    - При создании эпиков и задач, ветки должы автомтически создаваться, кнопки нужно стобы удостовериться человеку и в случае сбоев он мог продвинуть задачи или эпики.
    - При выполнении работ по задче, автоматические закоммичивание, создание мр в эпик, и заливание ветки задачи в эпик автоматически.
    - При заврешении всех задач эпика, ветка эпика собирается и готовится к сливанию в mian ветку по клику пользователя, до этого должы быть уже решены конфлиты ветки эпика и главйно ветки в ветке эпика.
DONE: Запуск оркестрации по кнопке не должно создавать новых задач или эпиков, не должно писать сособщние в чат, эта кнопка просто берет в работу эпики и задачи которые есть в доске. Если нет задач и эиков которые можно брать в работу, то в режиме одждания.
TODO: В экспорте логов и чатов нужно добавить timestamp в начале названия файлов.
TODO: Для создания эпиков через чат, ассистент должен собрать сводку из обязательных параметров эпика:
    - требуется ли ревью архитектора, если нет, то понятно ли к какому лиду эпик относится (опционально "спросить у пользователя")
    - также с дргими обязательными параметрами
TODO: Для создания задачи через чат, ассистент должен собрать сводку из обязательных параметров задачи:
    - Задача должна создавать в рамках какого либо эпика, из контекста можно автоматичесик определять (опционально "спросить у пользователя")
    - Задача должна быть назначена для специалиста, из контекста можно автоматичесик определять, если может затронуть разных специалистов то лучше переводить в эпик и через архитектора сделать (опционально "спросить у пользователя")
    - также с дргими обязательными параметрами
TODO: Привести в порядок процесс работы доски:
    - Кнопока Стоп/продолжить должна быть неким тикером, от корого зависит работа осталных компонентов
    - Доска и чат соприкасаются между собой и этот механизм нужно грамотно перероектировать (например патерном события/слушатели или какой лучше подойдет)
    - главное чтобы код и логика были поддерживаемыми и легко дополняемыми
TODO: сделать анализ и составить отчет в .md формате о том как рабоатет доска с эпиками, задачами, лидами, специалистами и аналитиками а также гитфлоу. Дать рекомендации по улучшению, может нужно отказаться от эпиков? или заести новые сущности логики?